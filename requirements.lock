# NeuroTCS v1.7.12 — locked dependency versions
# ================================================
#
# These are the EXACT versions used to produce the AD validation locked
# invariants (audit_ids and cTCS values) in docs/datasheet/ad_neurotcs_datasheet.md
# Section A. An external collaborator wanting to verify those invariants
# bit-exactly should install these versions, NOT the floors in pyproject.toml.
#
# Python: 3.12.3 (Ubuntu 24.04 stock build, GCC 13.3.0)
# Generated:  2026-05-18 (AD-lock Step 2.4)
# Verified:  331 passed, 2 skipped on sandbox + cold-install of packaged zip
#
# Usage:
#     pip install -r requirements.lock --break-system-packages
#     pip install -e .
#     pytest tests/ -q   # expect: 331 passed, 2 skipped (or 333 with real
#                         #          MIRIAD CSVs + NEUROTCS_MIRIAD_DIR set)
#
# Floor versions live in pyproject.toml; floors are what NeuroTCS DECLARES as
# minimum compatible. Pins below are what NeuroTCS was TESTED with. The pins
# are the canonical reproducible environment for verifying locked invariants.

# Core data + schema stack
pydantic==2.13.4
PyYAML==6.0.3
pandas==3.0.2
pyarrow==24.0.0
jsonschema==4.26.0
pyreadr==0.5.6

# Numeric stack
numpy==2.4.4
scipy==1.17.1

# Test + lint
pytest==9.0.3
ruff==0.15.13

# NOTE: pytest-cov and fhir.resources are declared in pyproject.toml but are
# optional for verifying invariants. They are NOT pinned here because the
# AD-lock test suite does not require them. An external collaborator running
# just the verification pipeline can omit them; a developer extending the
# package should install them per pyproject.toml.
