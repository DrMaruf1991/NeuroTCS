# NeuroTCS v1.74.0 -- locked dependency versions
# Python: 3.12.x (project target; verified on 3.12.3)
# Generated: 2026-06-10
#
# SCOPE: this lockfile pins ONLY NeuroTCS's own dependency closure (core runtime
# + its actual transitive deps + the dev/CI toolchain). It is intentionally NOT
# a freeze of a shared/global environment -- a clean `pip install neurotcs`
# resolves exactly the runtime set below and nothing else. Versions are the
# resolved versions observed on the maintainer's machine (pip freeze 2026-06-10);
# the runtime closure was verified with importlib.metadata.requires().
#
# To regenerate canonically WITH HASHES (recommended for a release artifact):
#   pip install pip-tools
#   pip-compile --generate-hashes --extra dev -o requirements.lock pyproject.toml
#
# ============================================================================
# CORE RUNTIME (pyproject [project].dependencies)
# ============================================================================
pydantic==2.13.4
PyYAML==6.0.3
pandas==3.0.2
pyarrow==24.0.0
openpyxl==3.1.5
reportlab==4.5.1
jsonschema==4.26.0
numpy==2.4.4
scipy==1.17.1
pyreadr==0.5.6
pyreadstat==1.3.5
#
# ---- transitive runtime closure (verified via importlib.metadata) ----
pydantic_core==2.46.4
annotated-types==0.7.0
typing_extensions==4.15.0
typing-inspection==0.4.2
python-dateutil==2.9.0.post0
tzdata==2026.2
six==1.17.0
et_xmlfile==2.0.0            # openpyxl
pillow==12.3.0               # reportlab
charset-normalizer==3.4.7    # pyreadstat
narwhals==2.16.0             # pyreadr
jsonschema-specifications==2025.9.1
referencing==0.37.0
rpds-py==2026.5.1
attrs==26.1.0
#
# ============================================================================
# DEV / CI TOOLCHAIN (pyproject [dev] extra)
# ============================================================================
pytest==9.0.3
ruff==0.15.13
build==1.5.0
#
# ============================================================================
# SUPPLY-CHAIN / SBOM TOOLING (CI release gate only -- not a runtime dep)
# ============================================================================
pip_audit==2.10.0
cyclonedx-python-lib==11.6.0
#
# OPTIONAL EXTRAS (install on demand, NOT part of the core lock):
#   PDF input  : pip install 'neurotcs[pdf]'   (pdfplumber; gated by allow-pdf)
#   FHIR input : pip install 'neurotcs[fhir]'  (fhir.resources)
